import React from "react";

const ChannelForm = (props: {
  handleClickJoinAudience: any
  handleClickJoinHost: any
}) => {
  const { handleClickJoinAudience, handleClickJoinHost } = props;

  return (
    <form className="join">
      
    </form>
  );
};

export default ChannelForm;
